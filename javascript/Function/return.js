//if we want to store something from function then we need to use return 

function adder(num1,num2){
    return num1*num2
}

adder(5,5)//it will show nothing because we store value 
console.log(adder(5,5));